// Home.js
import React from 'react';
import classNames from 'classnames';
import { Outlet } from 'react-router-dom';

const Home = ({ children }) => {
  return (
    <div>
      sasd
      {children}
    </div>
  );
};

export default Home;